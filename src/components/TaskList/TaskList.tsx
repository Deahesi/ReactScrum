// import styles from './TaskList.module.css'
import {TaskListProps} from "./TaskList.props";
import cn from 'classnames'

export const TaskList = ({ list, className, ...props }: TaskListProps) => {
    return (
        <div>

        </div>
    )
}
